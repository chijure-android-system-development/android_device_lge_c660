<?xml version="1.0" encoding="utf-8"?>
<!--
    Overlay del C660 sobre frameworks/base/core/res/res/layout/usb_storage_activity.xml

    El bloque del boton usaba layout_alignParentBottom (anclado al fondo de
    la pantalla, sin relacion con el texto del mensaje arriba). En la
    pantalla de 240x320 (QVGA, ldpi) del C660 el texto largo del mensaje
    se solapaba con el boton "Turn on USB storage".

    Ademas el icono usb_android.png nativo en ldpi es 135x135px: mas del
    40% del alto de esta pantalla, dejando muy poco espacio para el resto.

    Fix: icono achicado a 100dp (en vez de scrollear el contenido) + boton
    anclado debajo del mensaje en vez de al fondo de la pantalla. Con el
    icono chico entra todo sin scroll. Mismo set de ids que el original
    (main, icon, banner, message, mount_button, unmount_button, progress):
    un id nuevo corre la numeracion de los ids internos privados
    (mount_button/banner no estan en public.xml) y rompe el SystemUI.apk
    ya compilado (confirmado con un crash real en hardware).
-->
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:gravity="center"
    android:padding="18dip"
    >

    <ImageView android:id="@+id/icon"
        android:layout_centerHorizontal="true"
        android:layout_alignParentTop="true"
        android:layout_width="100dip"
        android:layout_height="100dip"
        android:scaleType="fitCenter"
        android:src="@drawable/usb_android" />

    <TextView android:id="@+id/banner"
        android:layout_centerHorizontal="true"
        android:layout_below="@id/icon"
        android:layout_marginTop="10dip"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textSize="24sp"
        android:gravity="center"
        android:text="@string/usb_storage_title" />

    <TextView android:id="@+id/message"
        android:layout_below="@id/banner"
        android:layout_marginTop="10dip"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textSize="14sp"
        android:gravity="center"
        android:text="@string/usb_storage_message" />

    <RelativeLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerHorizontal="true"
        android:layout_below="@id/message"
        android:layout_marginTop="20dip"
        >

        <Button android:id="@+id/mount_button"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:paddingLeft="18dip"
            android:paddingRight="18dip"
            android:text="@string/usb_storage_button_mount"
            />
        <Button android:id="@+id/unmount_button"
            android:visibility="gone"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:paddingLeft="18dip"
            android:paddingRight="18dip"
            android:text="@string/usb_storage_stop_button_mount"
            />
        <ProgressBar android:id="@+id/progress"
            android:visibility="gone"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:indeterminate="true"
            style="?android:attr/progressBarStyle"
            />

    </RelativeLayout>
</RelativeLayout>
